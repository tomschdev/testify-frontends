// Consolidated onto the shared neo-brutalism primitives. Kept as a thin
// re-export so existing "@/components/primitives" import sites keep working —
// there is now one Badge and one Toggle across all three consoles.
export { Badge, Toggle } from "@attestant/ui";
